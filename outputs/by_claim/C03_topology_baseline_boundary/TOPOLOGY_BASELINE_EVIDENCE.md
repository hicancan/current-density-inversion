# Topology Baseline Evidence

Claim: `C03_unet_topology_baseline_boundary`.

| Evidence | Result | Boundary |
|---|---|---|
| topology-aware image inverse | Reduces topology residual within generated scope. | Baseline only. |
| residual via diagnostics | Residual-based diagnosis can outperform raw-field templates. | Thresholds and refusal must remain no-leakage. |
| no-via failure analysis | False positives remain a primary risk. | Not solved by topology loss alone. |
| return-path diagnostics | Return current is a separate physical mechanism. | Return-current inference is missing. |

Cannot claim: final reliable via detector or solved return-path diagnosis.

