"""Shared helpers for exp04 diagnostics.

The experiment script still owns orchestration and model training. This package
contains stable, side-effect-free helpers that are small enough to reuse in
tests and future experiments without turning `run_all.py` into a library.
"""

