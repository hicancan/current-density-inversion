# Notes

This experiment is the active `exp04` full benchmark. The earlier linear
topology-loss mechanism experiment is archived under
`experiments/archived/exp04-topology-soft-loss-network`.

The primary scientific question is:

> On the exp03 Biot-Savart two-layer via benchmark, does topology-aware learning
> reduce physical inconsistency without destroying reconstruction quality?

This experiment should be read together with:

- `exp03`: dataset generator and via/topology physics gate;
- archived `exp04`: topology soft loss mechanism proof;
- `exp05`: sensor nonideality stress gate;
- `exp06`: inverse-crime gate.
