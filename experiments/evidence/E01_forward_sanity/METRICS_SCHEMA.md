# Metrics Schema

`outputs/metrics.json` reports canonical Biot-Savart sanity metrics:

- field sign and linearity checks;
- finite-wire versus infinite-wire reference checks;
- via-field component sanity;
- standoff decay sanity;
- `all_acceptance_gates_passed`.

This evidence supports only ideal canonical free-space forward sanity.

